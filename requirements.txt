# متطلبات الاستضافة السحابية (Render وما شابه) — نسخة CPU فقط.
#
# محليًا استخدم install.ps1 بدلًا من هذا الملف — هو يثبّت PyTorch بدعم
# CUDA لكرت شاشتك. هذا الملف مخصّص لخادم بلا GPU.

# ── PyTorch (عجلات CPU القياسية من PyPI؛ لا حاجة لفهرس CUDA هنا) ──
torch==2.5.1
torchvision==0.20.1
torchaudio==2.5.1

# ── محرّك UVR / MDX / Roformer ──
# لا نُثبّت onnxruntime هنا: التثبيت المحلي يُقيّده على 1.20.2 خصيصًا
# للتوافق مع onnxruntime-gpu وCUDA 12.4 — قيد لا معنى له هنا (لا GPU،
# وأصلًا onnxruntime==1.20.2 غير منشور لحزمة CPU، هذا ما فجّر بناء
# Render). ندع audio-separator[cpu] يختار نسخة onnxruntime المتوافقة معه.
audio-separator[cpu]==0.44.5

# ── متطلبات Demucs v4 (بلا Demucs نفسه — يُثبَّت في Dockerfile بـ --no-deps) ──
# القائمة أدناه مطابقة لتبعيات Demucs الحقيقية كما اختبرتها محليًا. ملاحظة:
# ظننت في محاولة سابقة أن ترك demucs بلا --no-deps هو سبب سحب diffq (حزمة C
# تحتاج gcc، تفشل على صورة python-slim المجرَّدة) — لكن المصدر الفعلي كان
# audio-separator نفسها: تشترط diffq العادية على أي نظام غير ويندوز
# (diffq>=0.2; sys_platform != "win32") بينما تستخدم diffq-fixed (لها عجلة
# جاهزة) على ويندوز فقط — لهذا لم تظهر المشكلة محليًا إطلاقًا. الحل الآن في
# Dockerfile (تثبيت build-essential)، و--no-deps هنا يبقى احتياطًا جيدًا لا أكثر.
dora-search
einops
julius
lameenc
openunmix
pyyaml
tqdm
submitit
treetable
huggingface-hub
safetensors
sphn

# ── الواجهة والأدوات ──
gradio>=4.44,<6
soundfile
numpy
